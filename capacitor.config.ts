import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cartegraph.securestoragebug',
  appName: 'secure-storage-bug',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
